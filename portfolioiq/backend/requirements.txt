# The spec's original exact pins (fastapi==0.111.0, scipy==1.13.1, etc.)
# don't have prebuilt wheels for Python 3.13 and fail to build from source
# without a Fortran compiler installed. Loosened to ranges that resolve
# cleanly on 3.11 through 3.13.
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
numpy-financial>=1.0,<2
scipy>=1.13,<2
python-multipart>=0.0.9,<1
python-dotenv>=1.0,<2
pydantic>=2.7,<3
casparser>=1.3,<2
